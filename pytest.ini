[pytest]
minversion = 7.0
pythonpath = .
testpaths =
    tests
addopts = -ra -v --durations=10 --strict-markers --strict-config
markers =
    llm: spends a real model turn; slow and non-deterministic
    safety: red-team / guardrail check; a failure is a finding about the model
